para ver todas las imagenes
docker ps --all 

para ver logs 
docker logs --follow 4ce80093ac81

para ver app conectadas a mi red
docker network inspect minet

para app
docker stop festive_shamir

iniciar app
docker start elegant_nash


docker logs --follow heuristic_turing

borrar contenedor
docker rm  modest_hellman






borra imagenes
docker rmi gateway,auth   

para todos los contenedores
docker stop $(docker ps -q)


###COMANDOS PARA EJECUTAR LOS DOCKER FILE QUE ESTAN OPTIMIZADOS
Hace el mavn clean package -DskiptTest solo corriendo el comando de abajo
EJECUTAMOS DE LA CARPETA RAIZ FGSERVICEBACKEDN

MSCV-PANDORACENTER:
------------------
docker build --name pandoracenter-service -t pandoracenter-service . -f ./pandoracenter/pandora/Dockerfile

docker container run --name pandoracenter-service --network minet -d pandoracenter-service
 


MSCV-AUTH:
----------
docker build -t authservice:latest . -f ./auth2/auth/Dockerfile Dockerfile
docker container run --network minet  authservice:latest

MSVC-FARMER
-----------
docker build -t farmerservice:latest . -f ./farmer/farmerservices/Dockerfile

docker container run --network minet -d farmerservice:latest

MSCV-CHECKOUT
docker build  -t checkout-service . -f ./Checkoutpandora/checkout/Dockerfile
docker container run --name checkout-service --network minet -p8071:8071 -d checkout-service



MSVC-GATEWAY 
por el momento con el comando de abajo hay que actualizar
docker build  -t gateway-service . -f ./Dockerfile


docker container run --name gateway-service --network minet -p8080:8080 -d gateway-service









